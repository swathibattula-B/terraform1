resource "aws_route53_record" "www" {
  zone_id = var.zone_id
  name    ="${var.aws_instances[count.index]}.${var.domain_name}"
  type    = "A"
  ttl     = 300
  records = [aws_instance.example[count.index].private_ip]
}